import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const completedBoxColor = Colors.green;
  static const completedTextColor = Color(0xFFAAAAAA);
  static const errorColor = Colors.red;
  static const defaultTextColor = Colors.black;
  static const editColor = Colors.grey;
}